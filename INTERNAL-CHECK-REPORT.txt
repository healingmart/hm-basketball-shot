Basketball Shot v30 internal checks

- Based on uploaded latest HTML.
- Blogger mobile wrapper changed to position:fixed + inset:0 + max z-index.
- Page header/content is covered on mobile; browser chrome remains until Fullscreen button is tapped.
- Added in-game Fullscreen button using Fullscreen API (iframe has allowfullscreen).
- Mobile layout uses viewport/safe-area, not fixed device width.
- Portrait, narrow-phone, short-height and landscape breakpoints added.
- status-text and aim-guide globally separated to remove overlap.
- Overlay menus scroll inside viewport instead of clipping.
- ResizeObserver fallback included.

Additional Blogger overlay hardening:
- Mobile iframe wrapper is moved directly under document.body.
- This escapes transformed/stacking-context post containers in Blogger themes.
- z-index raised to 2147483647.
- html/body scroll lock applied only on mobile.
